
name: "Update README with python script and ffmpeg"

# Controls when the workflow will run
on:
  # run automatically every 24 hours
  schedule:
    - cron: "* */24 * * *" 
  push:
    branches:
      - 'main'


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install matplotlib numpy
      - name: Run python script
        run: python update_readme.py
      - name: Setup FFmpeg
        uses: AnimMouse/setup-ffmpeg@v1
      - run: ffmpeg -y -i animation.gif -filter_complex "[0:v]reverse,fifo[r];[0:v][r] concat=n=2:v=1 [v]" -map "[v]" animationfull.gif
      # Push the changes
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          force: true
#- name: Commit and push
#  run: |
#    git config user.name github-actions
#    git config user.email github-actions@github.com
#    git add animation.gif
#    git add animationfull.gif
#    git add README.md
#    git commit -m "Update README.md with dynamic content"
#    git push origin main
